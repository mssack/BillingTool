<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:local="clr-namespace:BillingTool.Themes.Controls">
	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="/CsWpfBase;component/Themes/Controls/_shared/Styles.xaml" />
		<ResourceDictionary Source="/BillingTool;component/Themes/Styles/uitext/BelegData.xaml" />
	</ResourceDictionary.MergedDictionaries>

	<Style TargetType="{x:Type local:BelegDataView}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:BelegDataView}">
					<Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
						<Grid DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Item}">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<Border>
								<Grid>
									<Grid.Resources>
										<Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
											<Setter Property="Margin" Value="3,0,3,0"/>
										</Style>
									</Grid.Resources>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="Auto"/>
									</Grid.ColumnDefinitions>
									<TextBlock Grid.Column="0" Text="{Binding Path=Nummer}"/>
									<TextBlock Grid.Column="1" Text="-"/>
									<TextBlock Grid.Column="2" Text="{Binding Path=Datum}"/>
								</Grid>
							</Border>
							<Border Grid.Row="1">
								<Grid>
									<Grid.RowDefinitions>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
									</Grid.RowDefinitions>

									<ParamView Grid.Row="0" Header="{StaticResource Bez-BelegData-Datum}" Value="{Binding Path=Datum}"/>
									<ParamView Grid.Row="1" Header="{StaticResource Bez-BelegData-State}">
										<ParamView.Value>
											<MultiBinding Converter="{StaticResource GConv-MultiBindingStringFormat}" ConverterParameter="{}{0} - {1}">
												<Binding Path="Typ" Converter="{StaticResource GConv-GetEnumName}"/>
												<Binding Path="State" Converter="{StaticResource GConv-GetEnumName}"/>
											</MultiBinding>
										</ParamView.Value>
									</ParamView>
									<ParamView Grid.Row="2" Header="{StaticResource Bez-BelegData-State}">
										<ParamView.Value>
											<MultiBinding Converter="{StaticResource GConv-MultiBindingStringFormat}" ConverterParameter="{}{0} ({1})">
												<Binding Path="KassenOperator"/>
												<Binding Path="KassenId"/>
											</MultiBinding>
										</ParamView.Value>
									</ParamView>
									<ParamView Grid.Row="3" Header="{StaticResource Bez-BelegData-Empfänger}" Value="{Binding Path=Empfänger}"/>
									<ParamView Grid.Row="4" Header="{StaticResource Bez-BelegData-EmpfängerId}" Value="{Binding Path=EmpfängerId}"/>
									<ParamView Grid.Row="5" Header="{StaticResource Bez-BelegData-Comment}" Value="{Binding Path=Comment}"/>
								</Grid>
							</Border>
						</Grid>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>